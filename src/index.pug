html
  head
    title Rss reader
  body
    div.progress.d-none
      div.progress-bar.progress-bar-striped.progress-bar-animated(
        role='progressbar'
        aria-valuenow='100'
        aria-valuemin='0'
        aria-valuemax='100'
        style='width: 100%'
      )
    div.alert-container
    div.jumbotron
      div.container
        h1.display-4 Rss reader
        p.lead This is a simple rss reader.
        hr.my-4
        form.search-form(novalidate)
          div.col-md-4.col-lg-5.pl-0.mb-3
            label.position-absolute(for='validationCustom01')
            input#validationCustom01.form-control.search-form-input.py-2(
              type='search'
              placeholder='Enter rss feed'
              required
            )
            div.invalid-feedback
          button.btn.d-block.btn-outline-success.col-lg-2(type='submit') Search
    div.container.rss-container
    div.modal(tabindex='-1', role='dialog')
      div.modal-dialog(role='document')
        div.modal-content
          div.modal-header
            button.close(type='button', data-dismiss='modal', aria-label='Close')
              span(aria-hidden='true') ×
          div.modal-body
          div.modal-footer
            button.btn.btn-secondary(type='button', data-dismiss='modal') Close
